<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.yumcoach.mapper.RefreshTokenMapper">

    <!-- Refresh Token 저장 -->
    <insert id="insertRefreshToken" parameterType="RefreshToken">
        INSERT INTO jwt_refresh (user_id, token, expires_at)
        VALUES (#{userId}, #{token}, #{expiresAt})
    </insert>
    
    <!-- Token으로 조회 -->
    <select id="findByToken" resultType="RefreshToken">
        SELECT id, user_id, token, expires_at
        FROM jwt_refresh
        WHERE token = #{token}
    </select>
    
    <!-- UserId로 조회 -->
    <select id="findByUserId" resultType="RefreshToken">
        SELECT id, user_id, token, expires_at
        FROM jwt_refresh
        WHERE user_id = #{userId}
        ORDER BY expires_at DESC
        LIMIT 1
    </select>
    
    <!-- Token 삭제 -->
    <delete id="deleteByToken">
        DELETE FROM jwt_refresh
        WHERE token = #{token}
    </delete>
    
    <!-- UserId로 삭제 -->
    <delete id="deleteByUserId">
        DELETE FROM jwt_refresh
        WHERE user_id = #{userId}
    </delete>

</mapper>
