# ==========================================
# Docker Compose 환경변수 템플릿
# ==========================================
# 이 파일을 .env로 복사하고 실제 값을 입력하세요

# 환경 모드 (local | docker)
# - local: 로컬에서 Spring/Vue 직접 실행 (MySQL만 도커)
# - docker: 모든 서비스를 도커로 실행
ENV_MODE=docker

# MySQL 설정
MYSQL_HOST=mysql
MYSQL_ROOT_PASSWORD=your_root_password_here
MYSQL_DATABASE=yumcoach_db
MYSQL_USER=yumcoach
MYSQL_PASSWORD=your_password_here

# MySQL 포트 (로컬 개발시 충돌 방지용)
MYSQL_PORT_LOCAL=3307
MYSQL_PORT_DOCKER=3306

# Spring Boot 포트
BACKEND_PORT_LOCAL=8081
BACKEND_PORT_DOCKER=8080

# Vue.js 포트
FRONTEND_PORT_VITE_DOCKER=3000
FRONTEND_PORT_NGINX_DOCKER=80

# 현재 사용할 포트 (ENV_MODE에 따라 변경)
# docker 모드: MYSQL_PORT=${MYSQL_PORT_DOCKER}
# local 모드: MYSQL_PORT=${MYSQL_PORT_LOCAL}
MYSQL_PORT=${MYSQL_PORT_DOCKER}
BACKEND_PORT=${BACKEND_PORT_DOCKER}
FRONTEND_PORT=${FRONTEND_PORT_VITE_DOCKER}

# API URL (환경에 따라 변경)
API_BASE_URL_LOCAL=http://localhost:${BACKEND_PORT_LOCAL}
API_BASE_URL_DOCKER=http://localhost:${BACKEND_PORT_DOCKER}
VITE_API_BASE_URL=${API_BASE_URL_DOCKER}

# Spring 프로파일
SPRING_PROFILES_ACTIVE=docker

# JWT Settings (Base64로 인코딩된 시크릿 키 - 최소 256bit)
# 예시: echo -n "your-secret-key-min-32-chars-long" | base64
JWT_SECRET_BASE64=your_jwt_secret_base64_encoded_here
JWT_ACCESS_TOKEN_VALIDITY=3600000
JWT_REFRESH_TOKEN_VALIDITY=604800000